<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WDCableWUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WDCableWUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WiFi Direct Cable - WDCable">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Custom Title Bar -->
        <Grid x:Name="CustomTitleBar" Grid.Row="0" Height="48" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <Image Source="Assets/Square44x44Logo.png" Width="16" Height="16" Margin="0,0,8,0"/>
                <TextBlock Text="WiFi Direct Cable" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Drag Region -->
            <Border Grid.Column="1" Background="Transparent" x:Name="DragRegion"/>
        </Grid>

        <!-- Main Content Area -->
        <NavigationView x:Name="MainNavigationView"
                       Grid.Row="1"
                       PaneDisplayMode="Left"
                       IsBackButtonVisible="Collapsed"
                       IsSettingsVisible="False"
                       OpenPaneLength="280"
                       CompactModeThresholdWidth="768"
                       ExpandedModeThresholdWidth="1008"
                       SelectionChanged="NavigationView_SelectionChanged">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Connection" Tag="Connection">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE968;"/>
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="ConnectionBadge" Visibility="Collapsed"/>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Chat" Tag="Chat">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8BD;"/>
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="ChatBadge" Visibility="Collapsed"/>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Speed Test" Tag="SpeedTest">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9F9;"/>
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="SpeedTestBadge" Visibility="Collapsed"/>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
                
                <NavigationViewItem Content="File Transfer" Tag="FileTransfer">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B5;"/>
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="FileTransferBadge" Visibility="Collapsed"/>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Header>
                <Grid Height="48" Margin="24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="PageTitle" 
                              Grid.Column="0"
                              Text="Connection"
                              Style="{StaticResource TitleTextBlockStyle}"
                              VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="1" 
                               Orientation="Horizontal" 
                               VerticalAlignment="Center"
                               Spacing="12">
                        
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                               CornerRadius="16"
                               Padding="12,6">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon x:Name="ConnectionStatusIcon"
                                         FontFamily="{StaticResource SymbolThemeFontFamily}"
                                         Glyph="&#xE703;"
                                         FontSize="14"
                                         Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                <TextBlock x:Name="ConnectionStatusText"
                                          Text="Disconnected"
                                          FontSize="12"
                                          FontWeight="Medium"
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </NavigationView.Header>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame" 
                   Margin="0,12,0,0"
                   Background="Transparent"/>
        </NavigationView>

        <!-- Status Bar -->
        <Border Grid.Row="2"
               Background="{ThemeResource LayerFillColorDefaultBrush}"
               BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
               BorderThickness="0,1,0,0">
            <Grid Height="32" Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusMessage"
                          Grid.Column="0"
                          Text="Ready"
                          FontSize="12"
                          VerticalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                <TextBlock x:Name="DeviceInfo"
                          Grid.Column="1"
                          Text="No device connected"
                          FontSize="12"
                          VerticalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
